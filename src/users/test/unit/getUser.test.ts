describe('GetUser', () => {
    test.todo('should return user from database if user exists');
    test.todo('should throw an error if user does not exist');
    test.todo('should return user with social media if user has social media');
    test.todo('should return user with programs if user has programs');
    test.todo(
        'should return user with undisclosed program if user has no programs'
    );
});
